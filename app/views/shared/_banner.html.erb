<div class="banner mb-3" style="<%= "background-image: url(#{image_path(background_image)});" if background_image.present? %>">
  <div class="banner-content">
    <h1 class="banner-title"><%= title %></h1>
  </div>
</div>
